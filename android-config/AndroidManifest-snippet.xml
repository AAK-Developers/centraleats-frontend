<!--
  Edita android/app/src/main/AndroidManifest.xml (generado por Capacitor)
  y agrega el atributo android:networkSecurityConfig al tag <application>.

  Antes:
    <application
        android:allowBackup="true"
        ...>

  Después (agregar esta línea):
    <application
        android:allowBackup="true"
        android:networkSecurityConfig="@xml/network_security_config"
        ...>
-->
